import breakpoints from '../breakpoints';

export default {
    breakpoints,
    googleAds: {
        slotPrefix: 'gpt-slot-' // Probably can be removed, but need to look investigate more to be sure
    }
};
